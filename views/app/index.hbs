<h2>This is the Index page</h2>

{{!-- {{#each user }}
<p>
    {{ name }}
    {{ weight }}
    <a href="{{_id}}"><button>Show Profile</button></a>
</p>

{{/each}} --}}
{{#each user}}
<div class="row">
    <div class="col s12 m6">
        <div class="card blue-grey darken-1">
            <div class="card-content white-text">
                <a href={{ _id }}><span class="card-title"> {{ name }} </span></a>
                <p>{{ weight }}</p>
            </div>
        </div>
    </div>
</div>


{{/each}}